streamlit>=1.37.0
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0
plotly>=5.18.0
OrcFxAPI>=11.6.0; sys_platform == "win32"

# OrcFxAPI (PyPI) installs OrcFxAPI.py / OrcFxAPIConfig.py, which need Windows and a
# licensed local OrcaFlex install to load OrcFxAPI.dll. It is skipped on Linux/macOS
# (e.g. Streamlit Community Cloud), where extractions cannot run at all.
