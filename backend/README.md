# OrcaFlex extraction library

`var_and_func/` is imported by the Streamlit dashboard
(`streamlit_app/utils/loaders.py`). It is not a standalone HTTP server.

Run the app with:

```bash
streamlit run streamlit_app/app.py
```

OrcFxAPI comes from the local OrcaFlex install and is not on PyPI.
